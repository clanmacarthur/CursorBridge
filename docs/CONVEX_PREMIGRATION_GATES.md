# Convex Pre-Migration Gates

Last updated: 2026-02-24

Purpose:
- Define the exact gates that must be green before we start Convex import.
- Keep direction stable and avoid accidental scope drift.

## Current readiness

Overall status: **PASS (Batch 1 complete)**.

Why:
- Mapping exists.
- Export snapshot exists.
- Convex schema is prepared locally.
- Local dry-run and local parity passed.
- Deployment link and import are complete.
- Basic runtime sanity check passed.

Live checkpoint (2026-02-23):
- Mapping gate: **PASS**
- Export gate: **PASS**
  - snapshot: `exports/convex_stage_seed/20260223T184804Z`
  - manifest copy: `docs/_convex_stage_export_manifest_latest.json`
- Convex schema gate: **PASS**
  - `C:\code\Regenerative-Hive-Mind\convex\schema.ts`
- Dry-run gate: **PASS (local simulation)**
  - report: `docs/_convex_stage_readiness_audit.json`
- Parity gate: **PASS (local seed parity)**
  - seed manifest: `exports/convex_stage_seed/20260223T184804Z/convex_seed/manifest_convex.json`
- Runtime sanity gate: **PASS (collection visibility + sample reads + Convex query snapshot)**
- Convex deployment connection gate: **PASS**
  - project: `regenerative-hive-mind`
  - dev deployment: `elated-ibis-363`

## Gates (must all pass)

1. Mapping gate
- `docs/CONVEX_STAGE_COLLECTION_MAP.md` approved and frozen for import batch 1.

2. Export gate
- Stage export snapshot generated by `scripts/export_stage_tables_for_convex.py`.
- Manifest file exists and row totals match source snapshot.

3. Convex schema gate
- Collections defined for all 12 mapped tables.
- Required indexes defined (at least on `notionPageId`).

4. Dry-run gate
- Import simulation has no fatal errors.
- Re-run is idempotent at transform/check stage.

5. Parity gate
- Seed row counts match source export rows (after filtering missing ids).

6. Runtime sanity gate
- Imported collections are visible via CLI.
- Sample reads (`limit 1`) return non-empty payloads for all 12 imported collections.
- App-level readiness query returns healthy snapshot:
  - `api.readiness.batch1ReadinessSnapshot`
  - `allNonEmpty = true`
- Session lookup query layer returns valid payloads:
  - `api.sessions.sessionLookupSafety`
  - `api.sessions.sessionLookupBreathwork`
  - `api.sessions.sessionLookupBuilderControls`
  - `api.sessions.sessionLookupThemes`

7. Convex deployment connection gate
- Convex account login and project link completed.
- `convex dev` ran successfully from workspace.

## Stop conditions

Stop and review if any of these happen:
- Import duplicates by `notionPageId`.
- Row mismatch after dry-run/parity checks.
- Required fields missing in seed docs.
- Session critical queries regress.
- Convex project not linked (deployment commands blocked).

## Recommended order now

1. Keep Supabase freeze in place (no major write batches).
2. Add app-level read checks for imported Convex collections.
3. Prepare next mapping batch only after those checks pass.
4. Keep import history notes for each batch and deployment.
